# Terraform state files
*.tfstate
*.tfstate.backup

# Crash log from Terraform
crash.log

# Ignore the .terraform directory, which contains provider binaries, modules, and plugins
.terraform/

# Ignore override files and secrets (these usually contain sensitive data)
override.tf
override.tf.json
*.tfvars
*.tfvars.json

# Ignore Terraform plan output files (binary file generated by terraform plan -out)
*.tfplan

# Ignore any local workspace files
.terraform.tfstate.d/

# Lock files (if you're not versioning provider dependencies in the repository)
.terraform.lock.hcl

# Ignore archive or package files (like Lambda zip files)
*.zip

# Editor or OS-specific files
.DS_Store
*.swp
*.swo
Thumbs.db
